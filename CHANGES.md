# Change Log

| Timestamp | File | Action | Requirement |
|-----------|------|---------|-------------|
| 2026-04-24 | DB/Repositories/Interfaces/IMasterDataRepository.cs, DB/Repositories/MasterDataRepository.cs, BusinessLogic/Interfaces/IMasterDataService.cs, BusinessLogic/Services/MasterDataService.cs, Api/Controllers/MasterDataController.cs | Added SaveMaterialBudgets endpoint and SaveMaterialBudgetsAsync method across Controller/Service/Repository layers accepting a list (single or multiple items) | Support bulk material budget creation without altering existing single-item SaveMaterialBudget flow |
| 2026-04-14 | DB/Model/VendorCategoryChangeLog.cs | Created audit entity for tracking vendor category code changes | Enforce max 2 changes within 3-year validity period — requires change history to count against |
| 2026-04-14 | DB/Model/ProcuraDbContext.cs | Added DbSet\<VendorCategoryChangeLog\> | Register new audit table in EF Core context |
| 2026-04-14 | BusinessLogic/Models/CategoryChangeValidationResult.cs | Created structured validation result DTO | Return freeze period, change count, eligibility status to controller/UI for header-level pre-validation |
| 2026-04-14 | DB/Repositories/Interfaces/IVendorRepository.cs | Added GetCategoryChangeCountAsync and LogCategoryChangeAsync | Repository contracts for change tracking and audit logging |
| 2026-04-14 | DB/Repositories/VendorRepository.cs | Implemented change count query, audit logging, and auto-log on SaveCategoriesAsync | Enforce change-limit rule and maintain audit trail inside existing transaction |
| 2026-04-14 | BusinessLogic/Interfaces/IVendorService.cs | Added ValidateCategoryChangeAsync to interface | Service contract for header-level category change eligibility check |
| 2026-04-14 | BusinessLogic/Services/VendorService.cs | Implemented ValidateCategoryChangeAsync with freeze period and change-limit checks; injected IMasterDataRepository | Enforce 6-month freeze after approval and max 2 changes within N-year validity at service layer |
| 2026-04-14 | Api/Controllers/VendorController.cs | Removed ValidateCategoryChangeEligibility endpoint and pre-validation gate; Categories endpoint now only for initial registration | Approval workflow moved to dedicated CategoryCodeApprovalController; direct saves restricted to pre-approval vendors |
| 2026-04-14 | DB/Migrations/20260414014625_AddVendorCategoryChangeLog.cs | EF Core migration for VendorCategoryChangeLogs table | Create audit table with FK to Vendors and index on VendorId |
| 2026-04-14 | DB/Model/CategoryCodeApproval.cs | Created approval request entity with Status (Pending/Approved/Rejected), RequestedData JSON snapshot, ReviewedBy FK to Users | Vendor category changes after SAP approval must go through a controlled approval workflow — no direct writes to VendorCategory |
| 2026-04-14 | DB/Model/CategoryCodeApprovalItem.cs | Created line-item entity for individual categories within an approval request | Enable queryable, FK-constrained category items per approval request for validation and review |
| 2026-04-14 | DB/Model/ProcuraDbContext.cs | Added DbSets for CategoryCodeApproval and CategoryCodeApprovalItem; configured relationships in OnModelCreating | Register approval workflow tables with proper FK constraints (Vendor cascade, User NoAction, Items cascade) |
| 2026-04-14 | DB/Entity/CategoryCodeApprovalDto.cs | Created DTOs: CategoryCodeApprovalDto, CategoryCodeApprovalItemDto, CategoryCodeApprovalListDto, ApproveCategoryChangeRequest, RejectCategoryChangeRequest | Support approval list grid, detail page, and approve/reject API contracts |
| 2026-04-14 | DB/Profiles/CategoryCodeApprovalProfile.cs | Created AutoMapper profile for CategoryCodeApproval and CategoryCodeApprovalItem | Bidirectional mapping between approval entities and DTOs |
| 2026-04-14 | DB/Repositories/Interfaces/ICategoryCodeApprovalRepository.cs | Created repository interface with Create, List, GetById, HasPending, Approve, Reject, GetPendingCount methods | Data access contracts for the full approval lifecycle |
| 2026-04-14 | DB/Repositories/CategoryCodeApprovalRepository.cs | Implemented full approval repository: create request with file handling, list/detail queries with joins, transactional approve (insert VendorCategory + certificates + change log), reject with reason | Core data layer for approval workflow — approval inserts into VendorCategory, rejection only updates status |
| 2026-04-14 | DB/DB.csproj | Added Compile Include entries for ICategoryCodeApprovalRepository.cs and CategoryCodeApprovalRepository.cs | Repository files excluded by blanket Compile Remove pattern; must be explicitly included |
| 2026-04-14 | BusinessLogic/Interfaces/ICategoryCodeApprovalService.cs | Created service interface: SubmitRequest, GetRequests, GetRequestById, Approve, Reject, ValidateEligibility | Service contract for approval workflow business logic |
| 2026-04-14 | BusinessLogic/Services/CategoryCodeApprovalService.cs | Implemented approval service with: duplicate pending check, eligibility validation, category/subcategory limit validation, re-validation at approval time | Enforce all business rules: max 2 categories, max 3 subcategories, freeze period, max 2 changes in validity period, one pending request at a time |
| 2026-04-14 | BusinessLogic/Services/VendorService.cs | Injected ICategoryCodeApprovalRepository; updated ValidateCategoryChangeAsync to count pending requests toward change limit | Prevent over-submission: pending requests count against the max 2 changes limit |
| 2026-04-14 | DB/Repositories/VendorRepository.cs | Removed auto-logging from SaveCategoriesAsync; logging now only on approval | Change log should only count approved changes, not direct saves during initial registration |
| 2026-04-14 | Api/Controllers/CategoryCodeApprovalController.cs | Created new controller with 6 endpoints: ValidateEligibility, SubmitRequest, GetRequests, GetRequestDetail, Approve, Reject | Dedicated API surface for the approval workflow with proper separation from VendorController |
| 2026-04-14 | Api/Program.cs | Registered ICategoryCodeApprovalService with DI container | Wire new approval service into the application |
| 2026-04-14 | DB/Program.cs | Registered ICategoryCodeApprovalRepository and CategoryCodeApprovalProfile | Wire new repository and AutoMapper profile into the DB layer |
| 2026-04-14 | DB/Migrations/20260414034437_AddCategoryCodeApprovalWorkflow.cs | EF Core migration for CategoryCodeApprovals and CategoryCodeApprovalItems tables | Create approval workflow tables with FKs to Vendors, Users, CodeMasters, Categories, SubCategories, Activities |
| 2026-04-16 | DB/Repositories/MasterDataRepository.cs | Rewrote SaveHierarchyAsync from delete-all/re-insert to upsert pattern with FK cleanup | Fix FK constraint violation (FK_VendorCategories_Activities_ActivityId) when saving hierarchy — VendorCategories, TenderCategoryCodes, and CategoryCodeApprovalItems reference Activities/SubCategories/Categories |
| 2026-04-16 | DB/Repositories/Interfaces/IVendorRepository.cs | Added IsRocNumberExistsAsync method to interface | Support duplicate ROC Number check during vendor registration |
| 2026-04-16 | DB/Repositories/VendorRepository.cs | Implemented IsRocNumberExistsAsync to check Vendors table by RocNumber | Prevent duplicate vendor accounts for the same company registration number |
| 2026-04-16 | BusinessLogic/Services/VendorService.cs | Added duplicate ROC Number validation in RegisterVendor before creating vendor | Block registration if vendor already exists — return clear error message to user |
| 2026-04-16 | Api/Controllers/VendorController.cs | Added try/catch in RegisterVendor endpoint to return BadRequest with error message on duplicate | Return user-friendly error: "Account already created for this ROC Number. Please login as Vendor" |
| 2026-04-16 | DB/EFModel/VendorFinancial.cs, DB/Entity/VendorFinancialDto.cs | Added `FileName` property for LatestBankStatement upload | Frontend needs to send the original filename alongside the base64 file data for Financial section uploads |
| 2026-04-16 | DB/EFModel/VendorBank.cs | Added `FileName` property for Bank Attachment upload | Frontend needs to send the original filename alongside the base64 bank statement attachment |
| 2026-04-16 | DB/EFModel/VendorCategoryCertificate.cs, DB/Entity/VendorCategoryCertificateDto.cs | Added `FileName` property for CertificatePath upload | Frontend needs to send the original filename alongside the base64 certificate file |
| 2026-04-16 | DB/EFModel/VendorExperience.cs, DB/Entity/VendorExperienceDto.cs | Added `FileName` property for AttachmentPath upload | Frontend needs to send the original filename alongside the base64 experience attachment |
| 2026-04-16 | BusinessLogic/Services/VendorService.cs | Mapped `FileName` in SaveFinancialAsync, SaveCategoriesAsync, SaveExperiencesAsync, and SaveStepAsync | Ensure FileName from DTO is passed through to entities for all file-upload registration sections |
| 2026-04-16 | DB/Model/BankKey.cs | Created BankKey master entity with Id, BankName, BankKeyCode | Bank Key dropdown on Vendor Bank Details form needs a master table of Malaysian bank names and their SAP bank key codes |
| 2026-04-16 | DB/Entity/BankKeyDto.cs | Created BankKeyDto for API layer | DTO for BankKey entity to cross layer boundaries |
| 2026-04-16 | DB/Profiles/BankKeyProfile.cs | Created AutoMapper profile for BankKey ↔ BankKeyDto | Bidirectional mapping between entity and DTO |
| 2026-04-16 | DB/Model/ProcuraDbContext.cs | Added DbSet\<BankKey\> BankKeys | Register BankKey table in EF Core context |
| 2026-04-16 | DB/Entity/DropDown.cs | Added BankKey to DropDown enum | Enable BankKey in generic dropdown API |
| 2026-04-16 | DB/Repositories/DropdownRepository.cs | Added BankKey handler returning bank names as dropdown items | Support generic dropdown API for bank key selection |
| 2026-04-16 | DB/Repositories/Interfaces/IMasterDataRepository.cs | Added GetAllBankKeysAsync method | Repository contract for fetching full bank key data (name + code) |
| 2026-04-16 | DB/Repositories/MasterDataRepository.cs | Implemented GetAllBankKeysAsync | Return all bank keys ordered by name with full DTO (includes BankKeyCode) |
| 2026-04-16 | BusinessLogic/Interfaces/IMasterDataService.cs | Added GetAllBankKeysAsync to interface | Service contract for bank key data |
| 2026-04-16 | BusinessLogic/Services/MasterDataService.cs | Implemented GetAllBankKeysAsync delegating to repository | Wire service layer for bank key data |
| 2026-04-16 | Api/Controllers/MasterDataController.cs | Added GetAllBankKeys GET endpoint with try-catch | Expose `GET /api/MasterData/GetAllBankKeys` for Bank Key dropdown on vendor bank details form |
| 2026-04-16 | DB/Migrations/20260416074433_AddBankKeyMasterTable.cs | Migration: create BankKeys table + seed 24 Malaysian bank entries | Populate Bank Key master data from Excel reference sheet |
| 2026-04-11 | DB/Repositories/Interfaces/IRolePermissionRepository.cs | Added `GeteRolePermissionAsync(int PermissionId)` to interface | Method existed in the implementation but was missing from the interface, causing a compiler contract violation |
| 2026-04-11 | BusinessLogic/Interfaces/IRoleMenuPermissionService.cs | Added `GetRolePermissionAsync(int permissionId)` to service interface | Expose get-by-id capability through the service layer |
| 2026-04-11 | BusinessLogic/Services/RoleMenuPermissionService.cs | Implemented `GetRolePermissionAsync` delegating to repository | Wire service layer to repository for get-by-id permission lookup |
| 2026-04-11 | Api/Controllers/AdminController.cs | Added `GetRolePermissionById` GET endpoint | Expose `GET /api/Admin/GetRolePermissionById?permissionId={id}` so clients can fetch a single role menu permission |
| 2026-04-10 | DB/Repositories/VendorRepository.cs | Added base64-to-file save logic for `VendorBank.Attachment` in `SaveFinancialAsync` | Bank statement attachment must be saved as a physical file under `C:\VendorUploads\` and only the file path stored in the DB, consistent with how `LatestBankStatementPath` is handled |
| 2026-04-10 | DB/Model/VendorBank.cs | Added `BankBranch`, `BankBranchAddress`, `Balance`, `FixedDeposit`, `Attachment` fields | Vendor registration Financial step requires Bank Branch, Bank Branch Address, Balance (RM last 3 months), Fixed Deposit (RM), and bank statement attachment as per the UI screen |
| 2026-04-06 | DB/Profiles/UserProfile.cs | Fixed AutoMapper mappings for `User → UserDTO`: resolved `int SiteOffice → State SiteOffice` type crash; added explicit mappings for `Email`, `Mobile`, `SiteOfficeId`, `SiteOfficeName`, navigation property ignores on reverse map | `GetUserById` threw `AutoMapperMappingException: Missing type map Int32 → State` because `User.SiteOffice` (int FK) and `UserDTO.SiteOffice` (State nav property) share a name but have incompatible types |
| 2026-04-03 | DB/Entity/UserDTO.cs | Added `SiteLevelName`, `SiteOfficeName`, `DesignationName` string properties | User list API response was missing site level, site office, and designation data |
| 2026-04-03 | DB/Repositories/UserRepository.cs | Updated all 4 user query projections to map `SiteLevelId`, `SiteOfficeId`, `DesignationId`, and name fields; added `Include(x => x.Designation)` to all queries | User list API response was missing site level, site office, and designation data |
| 2026-04-17 | DB/Helper/EmailHelper.cs | Rewrote `GetWelcomeEmailBody` HTML template: FPMSB PROCURA branded header (red #B84521), Felda Plantation Management Sdn. Bhd. subtitle, Staff ID / Temporary Password labels, "Login to FPMSB PROCURA" CTA, 4-step login instructions, and FPMSB footer with support contact | Match the approved welcome-email design in `FlowScreens-phase2/EmailTemplate.png` so newly created staff receive correctly branded account credentials |
| 2026-04-16 | CLAUDE.md (root) | Created project-level CLAUDE.md documenting both frontend and backend architecture, conventions, and Phase 2 rules | Provide Claude Code with persistent project context across sessions |
| 2026-04-16 | FledaPlantationManagement_Frontend/.env | Created `.env` with `VITE_API_BASE_URL=https://localhost:7225` | Frontend had no API base URL configured — all API calls would fail |
| 2026-04-16 | DB/Repositories/DropdownRepository.cs | Added TenderCategory handler to SelectList API | TenderCategory was in the DropDown enum but had no handler — dropdown returned empty list |
| 2026-04-16 | DB/Repositories/DropdownRepository.cs | Added TenderApplicationStatus handler to SelectList API | TenderApplicationStatus was in the DropDown enum but had no handler — status filter dropdown on listing page was empty |
| 2026-04-16 | DB/Model/MaterialBudget.cs, DB/Entity/MaterialBudgetDto.cs | Replaced 3 columns (WBSReference, CostCentreReference, IOReference) with 2 normalized columns (RujukanType, RujukanValue) | Normalize rujukan fields — single dropdown + value instead of 3 separate nullable columns |
| 2026-04-16 | DB/Repositories/MasterDataRepository.cs | Updated Add/Update/Get methods for MaterialBudget to use RujukanType + RujukanValue | Support normalized rujukan fields in all CRUD operations |
| 2026-04-16 | Api/Controllers/MasterDataController.cs | Added rujukan validation on SaveMaterialBudget and UpdateMaterialBudget; added `[FromQuery]` to GetTenderApplicationBySearch | Validate rujukanType is WBS/COST_CENTRE/IO; fix search endpoint not binding query params |
| 2026-04-16 | FledaPlantationManagement_Frontend/src/apis/apiEndpoints.ts | Added 8 tender application endpoints, 4 approval workflow endpoints, 5 announcement endpoints | Connect frontend to all backend tender, workflow, and news APIs |
| 2026-04-16 | FledaPlantationManagement_Frontend/src/apis/apiService.ts | Added 20+ API functions: tender CRUD, material budget, approval workflow, announcements, committee users | Complete frontend API service layer for Phase 2 features |
| 2026-04-16 | FledaPlantationManagement_Frontend/src/app/pages/TenderFormPage.tsx | Full rewrite: static shell → dynamic form with API integration, cascading Job Scope dropdowns from MaterialBudget, category code modal with hierarchy, site visit, required documents, created-by from JWT user, dynamic approval workflow sections with role-based enable/disable | Phase 2 Tender Application form with full CRUD, wizard tabs, and approval workflow |
| 2026-04-16 | FledaPlantationManagement_Frontend/src/app/routes.tsx | Added `tender-form/:id` route for edit mode | Support editing existing tenders by navigating with ID param |
| 2026-04-16 | FledaPlantationManagement_Frontend/src/app/pages/TenderManagementPage.tsx | Full rewrite: dummy data → real API integration with filter dropdowns, search, status resolution via lookup maps, TenderCode display, click-to-edit navigation | Phase 2 Tender Management listing page with real data |
| 2026-04-16 | DB/Model/TenderApplication.cs | Added `TenderCode` (string), changed `TenderCategoryCode` from single nav to `ICollection<TenderCategoryCode> TenderCategoryCodes` | Auto-generated reference ID; fix 1:1 relationship that prevented saving multiple category codes |
| 2026-04-16 | DB/Entity/TenderApplicationDto.cs | Added `TenderCode` property | Expose auto-generated tender reference ID in API responses |
| 2026-04-16 | DB/Repositories/TenderRepository.cs | Auto-generate TenderCode as `{yyyy}/T{Id:D4}` after insert; manual load of category codes via direct DbSet query (bypassing broken 1:1 navigation); replaced complex category code upsert with delete-all + re-insert; added 4 approval workflow methods (Initialize, Get, ApproveReject, ChangeApprover) | Fix TenderCode generation, fix category code save/load for multiple rows, implement full approval workflow |
| 2026-04-16 | DB/Model/ProcuraDbContext.cs | Added `TenderApprovalWorkflows` DbSet; added fluent config for TenderCategoryCode 1:many relationship | Register new workflow table; fix EF 1:1 constraint that prevented multiple category codes per tender |
| 2026-04-16 | DB/Repositories/UserRepository.cs | Overrode `GetByIdAsync` with proper .Include() joins for SiteLevel, Designation, State, Role | Base RepositoryBase.GetByIdAsync used plain FindAsync with no joins — designation, department, mobile were null |
| 2026-04-16 | DB/Repositories/TenderRepository.cs | Updated `GetTendorReviewers` to use .Include() + .Select() projection instead of plain ToListAsync + AutoMapper | Reviewer/approver API returned null for designation, department, mobile due to missing navigation property joins |
| 2026-04-16 | Api/Controllers/TenderManagementController.cs | Added `[FromQuery]` to GetTenderApplicationBySearch; fixed GetTenderApplicationById to use query param not path segment; added 4 workflow endpoints (GetApprovalWorkflow, InitializeWorkflow, ApproveRejectWorkflow, ChangeWorkflowApprover) | Fix search binding, fix edit-by-ID, expose approval workflow API |
| 2026-04-17 | DB/Model/TenderApprovalWorkflow.cs | Created new entity: Id, TenderApplicationId, Stage, StageOrder, Level, DesignationId, AssignedUserId, Status, Remarks, ChangeRemarks, ActionDateTime | Unified approval workflow table replacing separate TenderReview + TenderApproval tables |
| 2026-04-17 | DB/Entity/TenderApprovalWorkflowDto.cs | Created DTO with joined user fields (AssignedUserName, AssignedUserDepartment, AssignedUserDesignation, AssignedUserMobile) | API response includes resolved user details for each workflow slot |
| 2026-04-17 | DB/Helper/Enums.cs | Added 8 new TenderApplicationStatusEnum values: ReviewApproved(7), ReviewReject(8), WilayahApproved(9), WilayahReject(10), HQApproved(11), HQReject(12), IssuanceApproved(13), IssuanceReject(14) | Granular status tracking per approval stage |
| 2026-04-17 | DB/Entity/AnnouncementDto.cs | Added Status (bool?), CreatedBy (string?), CreatedDate (DateTime?) | News listing needs status display, creator name, and creation timestamp |
| 2026-04-17 | DB/Repositories/AnnouncementRepository.cs | Updated all queries to include Status, Description, CreatedBy, CreatedDate; CreatedBy resolved to user FullName via Users table join; Add sets CreatedBy from JWT user | News listing showed empty CreatedBy (was storing userId not name); all fields needed for list and edit views |
| 2026-04-17 | FledaPlantationManagement_Frontend/src/app/pages/NewsManagement.tsx | Full rewrite: dummy data → real API CRUD via Announcement endpoints with type=1 (News); list with status filter, add/edit form with validation, delete with confirmation | Phase 2 News Management module with full CRUD |
| 2026-04-17 | FledaPlantationManagement_Frontend/src/app/pages/TenderFormPage.tsx | Added TenderIssuanceContent import; made wizard tabs clickable; "Tender Issuance" tab renders TenderIssuanceContent inside same page; "Proceed Tender Issuance" button switches tab instead of navigating away | Fix missing header tabs when viewing issuance — both tabs now render within TenderFormPage |
| 2026-04-17 | FledaPlantationManagement_Frontend/src/app/pages/TenderFormPage.tsx | Approval workflow: sequential enforcement within stages (Level 1 must approve before Level 2 sees buttons); rejection stops entire stage; "Proceed Tender Issuance" button disabled until all stages approved | Business rule: approvals must be sequential, rejection halts flow, issuance only after full approval |
| 2026-04-18 | DB/Entity/ChangePasswordDto.cs | Created DTO with CurrentPassword, NewPassword, ConfirmPassword and required validation | Request payload for Change Password screen (Current / New / Retype New) |
| 2026-04-18 | DB/Repositories/Interfaces/IUserRepository.cs | Added ChangePasswordAsync(int userId, ChangePasswordDto dto) | Repository contract for changing the logged-in user password |
| 2026-04-18 | DB/Repositories/UserRepository.cs | Implemented ChangePasswordAsync: validates payload, matches current password, enforces password policy via ValidatePassword, checks password history (PWD_HISTORY policy), updates Users.Password, appends to UserPasswordHistories | Enforce current-password verification, password policy, and reuse-history rules at the data layer |
| 2026-04-18 | BusinessLogic/Interfaces/IUserService.cs | Added ChangePasswordAsync to service contract | Service layer expose of change-password use case |
| 2026-04-18 | BusinessLogic/Services/UserService.cs | Implemented ChangePasswordAsync delegating to repository | Keep controllers thin; business flow stays in service layer |
| 2026-04-18 | Api/Controllers/UserController.cs | Added [Authorize] POST ChangePassword endpoint; resolves userId from JWT "userid" claim; wrapped in try-catch returning CSAResponseModel<string> | API surface for the Change Password screen (FlowScreens-phase2/User/passwordchange.jpeg); must be authorized per Phase 2 rules |
| 2026-04-22 | DB/Model/Vendor.cs, DB/Entity/VendorProfileDto.cs | Added `IsActive` bool (default true) to Vendor entity and DTO | Introduce soft-delete flag so vendors can be marked inactive instead of physically removed |
| 2026-04-22 | DB/Repositories/VendorRepository.cs | Added `.Where(v => v.IsActive)` filter to GetVendorListAsync(), GetVendorDashboardAsync(), and GetVendorListAsync(VendorSearchRequest); tightened GetVendorByROCandPasswordAsync to filter by IsActive | All vendor listings and dashboard counts must exclude soft-deleted vendors; vendor-lookup-by-ROC used in login must reject inactive vendors |
| 2026-04-22 | DB/Repositories/VendorRepository.cs, DB/Repositories/Interfaces/IVendorRepository.cs | Added SoftDeleteVendorAsync(int vendorId): flips Vendor.IsActive to false; best-effort cascade to User.IsActive=false when a matching Vendor-role User exists (matched by RoleId=4 + RocNumber=UserName) | Replace hard delete with soft delete per business rule; cascade to User row so any User-level auth path is also blocked |
| 2026-04-22 | DB/Repositories/UserRepository.cs | Added `&& x.IsActive` to vendor credential check in CheckPassword | Block login attempts from deactivated vendors at the password-check layer |
| 2026-04-22 | DB/Repositories/TenderRepository.cs | Added `&& v.IsActive` filter when loading vendors by category for the tender opening page | Inactive vendors must not appear on tender opening / vendor performance pages |
| 2026-04-22 | BusinessLogic/Interfaces/IVendorService.cs, BusinessLogic/Services/VendorService.cs | Added SoftDeleteVendorAsync delegating to repository | Service contract and orchestration for the soft-delete endpoint |
| 2026-04-22 | Api/Controllers/VendorController.cs | Added `[Authorize] [HttpDelete("{vendorId:int}")] DeleteVendor` endpoint returning CSAResponseModel<bool>; wrapped in try-catch; returns NotFound when vendor missing | Expose HTTP DELETE /api/Vendor/DeleteVendor/{id} as a soft-delete operation per Phase 2 auth + response-wrapper rules |
| 2026-04-22 | Api/Controllers/AuthController.cs | Added defense-in-depth `!vendorDetails.IsActive` check in VendorAuthenticate after GetVendorFullDetailsAsync; logs auth attempt and returns Unauthorized with generic "Login failed" | Safety net so deactivated vendors cannot obtain a JWT even if ROC lookup were to bypass the IsActive filter |
| 2026-04-22 | DB/Migrations/20260422024736_AddVendorIsActive.cs | EF Core migration: AddColumn\<bool\> IsActive to Vendors table, bit not null, defaultValue=true | Preserve existing vendors as active during schema upgrade; new column must be NOT NULL to match the non-nullable C# property |
| 2026-04-22 | BusinessLogic/Validators/VendorCategoryLimitValidator.cs | Created shared static validator: EnsureLimits enforces max 2 categories per CodeMasterId and max 3 subcategories per category; throws Exception with clear message on first violation | Rule change: max category limit now scoped per CodeMasterId (top-level classification) instead of per vendor; shared by both the initial-registration path (VendorService.SaveCategoriesAsync) and the approval path (CategoryCodeApprovalService) so the rule has one source of truth |
| 2026-04-22 | BusinessLogic/Models/CategoryChangeValidationResult.cs | Added MaxCategoriesPerCodeMaster (int) and CategoryCountByCodeMaster (Dictionary\<int,int\>); retained MaxCategoriesAllowed for backward compatibility (now set to the per-CodeMaster value) | Expose per-slot state to the UI so it can show how many categories a vendor has under each CodeMaster and how many more they can add |
| 2026-04-22 | BusinessLogic/Services/VendorService.cs | Replaced MaxCategoriesAllowed constant with MaxCategoriesPerCodeMaster sourced from shared validator; SaveCategoriesAsync now loads vendor full details and calls VendorCategoryLimitValidator.EnsureLimits before saving; ValidateCategoryChangeAsync populates CategoryCountByCodeMaster | Enforce new per-CodeMaster limit on the initial-registration Categories endpoint (previously had zero payload-level validation); surface per-CodeMaster counts to UI via header-level eligibility response |
| 2026-04-22 | BusinessLogic/Services/CategoryCodeApprovalService.cs | Replaced inline ValidateCategoryLimits body with delegation to shared validator; updated constants to use validator constants; ValidateAndCheckEligibilityAsync now returns MaxCategoriesPerCodeMaster and CategoryCountByCodeMaster | Single source of truth for category limit rule; approval path and initial-registration path must not diverge |
| 2026-04-22 | DB/Repositories/TenderRepository.cs | Replaced `{yyyy}/T{Id:D4}` TenderCode format with `{prefix}/{yyMM}/{nnn}` (e.g., `KEJ/2604/001`). Added JobCategoryPrefixMap (Kontrak Kejuruteraan→KEJ, Kontrak Bekalan→BEK, Pertanian→PERT, Tapak Semaian→SEM, Pentadbiran→PTD, Bidaan Aset→AST) and GenerateTenderCodeAsync which looks up JobCategory.Name and allocates the next 3-digit running number by scanning existing TenderCodes with the same `{prefix}/{yyMM}/` prefix | Business rule: tender reference IDs must be human-readable, carry job-category context, and reset monthly. Prefix map is hardcoded for now — the 6 categories are fixed business values; adding a new category would require a code change |
| 2026-04-22 | DB/Model/User.cs, DB/Model/Vendor.cs | Added nullable `DateTime? LastLogin` column to both entities | Track per-user/per-vendor last successful login timestamp so the frontend header can display it and downstream reporting/security audits have the data |
| 2026-04-22 | DB/Entity/VendorProfileDto.cs | Added `DateTime? LastLogin` to VendorProfileDto | Surface the new Vendor.LastLogin through the existing AutoMapper profile without requiring mapping changes |
| 2026-04-22 | DB/Repositories/Interfaces/IUserRepository.cs, DB/Repositories/UserRepository.cs | Added `Task<DateTime?> UpdateLastLoginAsync(int userId)` that returns the previous LastLogin value, then sets it to DateTime.UtcNow and saves | Returning the previous value lets AuthController include it as a JWT claim so the client can render "last logged in on …" without an additional round-trip |
| 2026-04-22 | DB/Repositories/Interfaces/IVendorRepository.cs, DB/Repositories/VendorRepository.cs | Added `Task<DateTime?> UpdateLastLoginAsync(int vendorId)` with the same read-previous/write-current semantics | Same reason as above — mirror for the vendor login path |
| 2026-04-22 | BusinessLogic/Interfaces/IUserService.cs, BusinessLogic/Services/UserService.cs, BusinessLogic/Interfaces/IVendorService.cs, BusinessLogic/Services/VendorService.cs | Added `UpdateLastLoginAsync` delegating to the respective repositories | Keep Controller → Service → Repository layering; AuthController must not call the repository directly |
| 2026-04-22 | Api/Controllers/AuthController.cs | Authenticate() now calls `_userService.UpdateLastLoginAsync(user.Id)` before token generation; VendorAuthenticate() calls `_vendorService.UpdateLastLoginAsync(user.Id)` after the IsActive safety check. GenerateJwtToken/CreateClaims accept an optional `DateTime? lastLogin` and emit a new `"LastLogin"` claim containing the previous login timestamp in ISO 8601 (round-trip `"o"`) or empty string when null | Persist last-login timestamp on every successful login for both staff and vendor flows; carry the previous value in the JWT so the frontend header can display it by decoding the token (no extra API call needed). Update happens only after all pre-conditions pass (vendor IsActive check still runs first) so we don't record logins for denied requests. Parameters are optional to remain backward compatible with any internal callers |
| 2026-04-22 | DB/Migrations/ (run `dotnet ef migrations add AddLastLoginToUserAndVendor --project DB --startup-project Api`) | Pending EF Core migration adding nullable `LastLogin` (datetime2) columns to Users and Vendors tables | Schema must be migrated before deployment; nullable so existing rows don't need a backfill and the app continues to work for users who haven't yet logged in since the upgrade |
| 2026-04-23 | BusinessLogic/Models/Email/EmailMessage.cs | Created EmailMessage DTO (ToEmail, Subject, HtmlBody, IsHtml) | Shared payload for enqueue/send calls across the new email pipeline; no direct SMTP types leak into service layer |
| 2026-04-23 | BusinessLogic/Interfaces/IEmailService.cs, BusinessLogic/Services/EmailService.cs | Created IEmailService (EnqueueAsync, SendNowAsync, ProcessQueueAsync) and EmailService implementation that writes to EmailNotificationQueue and drains it via SMTP using existing Smtp:* appsettings | Centralizes SMTP plumbing (today duplicated across 6+ repositories with empty catches) and activates the dormant EmailNotificationQueue entity. Enqueue-first (transactional outbox) means SMTP outages don't fail vendor workflows |
| 2026-04-23 | DB/Model/ProcuraDbContext.cs | Registered DbSet\<EmailNotificationQueue\> EmailNotificationQueue | Required so EmailService can enqueue messages via the scoped DbContext; also unblocks the existing (compiled-out) ResidentUploadHistoryRepository reference |
| 2026-04-23 | DB/Helper/EmailHelper.cs | Added GetVendorRegistrationEmailBody(companyName, rocNumber, loginUrl) following the FPMSB card layout used by GetWelcomeEmailBody | First of 5 vendor email templates; placed in DB/Helper to avoid a circular Api↔BusinessLogic project reference and reuse the existing FPMSB template style |
| 2026-04-23 | Api/Workers/EmailOutboxWorker.cs | Created BackgroundService that polls IEmailService.ProcessQueueAsync every 60s via a per-iteration scope | Drains the outbox table asynchronously so request threads are not blocked by SMTP latency; scope per iteration keeps DbContext lifetime correct |
| 2026-04-23 | BusinessLogic/Interfaces/IVendorService.cs, BusinessLogic/Services/VendorService.cs | Added SendRegistrationConfirmationAsync(int vendorId). Injected IEmailService + IConfiguration + ILogger\<VendorService\> into VendorService constructor | Exposes a reusable, service-layer method to build and enqueue the vendor registration-received email. Exceptions are swallowed and logged — email failures must not break the caller's workflow |
| 2026-04-23 | BusinessLogic/Services/PaymentService.cs | Injected IVendorService and call SendRegistrationConfirmationAsync after SaveVendorPaymentAsync / SaveVendorPaymentMockAsync | "Successfully registered" means payment completed + IsRegistrationComplete=true; this is the correct trigger point per the vendor registration workflow |
| 2026-04-23 | Api/Program.cs | Registered IEmailService scoped and EmailOutboxWorker as HostedService | Wire the new services into DI; PaymentService/VendorService constructor changes are resolved by default |
| 2026-04-23 | Api/appsettings.json | Added AppSettings.VendorPortalUrl = "https://portal.fpmsb.com.my/login" | Used as the "Login to Track Status" CTA target in the registration email; env-overridable |
| 2026-04-23 | BusinessLogic/BusinessLogic.csproj | Added explicit PackageReference to Microsoft.Extensions.Configuration.Abstractions 7.0 and Microsoft.Extensions.Logging.Abstractions 7.0 | IConfiguration and ILogger\<T\> are now used in VendorService and EmailService; making the dependency explicit rather than relying on transitive flow keeps the build stable |
| 2026-04-23 | DB/Migrations/ (run `dotnet ef migrations add AddEmailNotificationQueue --project DB --startup-project Api`) | Pending EF Core migration creating the EmailNotificationQueues table (Id, RecipientEmail, Subject, Body, IsHtml, Status, RetryCount, MaxRetries, LastAttemptAt, CreatedAt, SentAt, ErrorMessage + BaseEntity audit columns) | The entity was previously defined but never mapped or migrated. Required before enqueue calls will persist; until the migration is applied the enqueue try/catch in SendRegistrationConfirmationAsync will log and swallow, so vendor flows continue to work |
| 2026-04-23 | DB/Helper/EmailHelper.cs | Added GetVendorApprovalEmailBody(companyName, vendorCode, approvedDate, expiryDate, loginUrl) following the FPMSB card pattern | Template for the "Registration Approved" email sent after SAP returns a vendor code. Shows issued Vendor Code, approval date, and 3-year validity end date |
| 2026-04-23 | DB/Repositories/Interfaces/IVendorRepository.cs, DB/Repositories/VendorRepository.cs | Added Task\<bool\> MarkVendorApprovedAsync(int vendorId, string vendorCode, DateTime approvalUtc) that sets VendorCode, VendorCodeStatus=Approved, ApprovalDatetime. Idempotent — returns false if the vendor is already approved with the same code | Today nothing flips VendorCodeStatus to Approved; SAPServices only persisted the request/response log. This method is the authoritative persistence step invoked by SAPServices on a successful SAP response (no ERROR rows and a VENDORCODE present) |
| 2026-04-23 | BusinessLogic/Interfaces/IVendorService.cs, BusinessLogic/Services/VendorService.cs | Added SendApprovalConfirmationAsync(int vendorId) that enqueues the approval email with company name, vendor code, approval date and computed expiry (ApprovalDatetime + 3 years). Exceptions are swallowed and logged so the calling SAP workflow cannot fail because of email | Expiry is computed on the fly from ApprovalDatetime in this commit; a persisted RegistrationExpiryDate column will be introduced with the renewal-reminders work |
| 2026-04-23 | BusinessLogic/Services/SAPServices.cs | Injected IVendorService + ILogger\<SAPServices\>. After SaveSAPRequestResponseAsync, inspect sapResponse.RETURN: if no row has TYPE="E" and a VENDORCODE is present, call IVendorRepository.MarkVendorApprovedAsync and then IVendorService.SendApprovalConfirmationAsync. Post-processing is wrapped in try/catch so the SAP response contract is unchanged on any persist/email failure | Completes the "vendor approved" event: SAP returns a code -> DB reflects Approved status -> approval email enqueued. Success detection is additive; the outer RespCode/RespDesc hardcoded values are preserved to maintain the existing API response shape |
| 2026-04-23 | DB/Helper/EmailHelper.cs | Added GetCategoryCodeApprovalEmailBody(companyName, vendorCode, approvedDate, categoryListHtml, loginUrl) with a dedicated .category-box block that renders an HTML bulleted list of CodeMaster » Category » SubCategory » Activity per approved item | Template for the "Category Code Change Approved" email. Shows vendor code, approval date, and the approved categories from the request's Items |
| 2026-04-23 | BusinessLogic/Interfaces/IVendorService.cs, BusinessLogic/Services/VendorService.cs | Added SendCategoryCodeApprovalEmailAsync(int vendorId, int approvalRequestId). Loads vendor + approval detail, builds an HTML-encoded `<li>` list of approved items via BuildCategoryItemListHtml (private), enqueues via IEmailService. Exceptions are swallowed and logged | Keeps all vendor-email composition inside VendorService. HTML encoding of names via System.Net.WebUtility.HtmlEncode prevents accidental markup injection from master-data names |
| 2026-04-23 | BusinessLogic/Services/CategoryCodeApprovalService.cs | Injected IVendorService. After await _approvalRepository.ApproveRequestAsync(requestId) succeeds, call IVendorService.SendCategoryCodeApprovalEmailAsync(approval.VendorId, requestId) | Fires the approval notification only after the approval is persisted. Because the VendorService method itself try/catches, a failed email cannot roll back or throw past the approval flow |
| 2026-04-23 | DB/Model/Vendor.cs | Added nullable DateTime? RegistrationExpiryDate | Anchor column for renewal reminders and for displaying validity in the approval email. Nullable so existing approved rows without expiry are simply skipped by the worker — no backfill required |
| 2026-04-23 | DB/Model/VendorReminderLog.cs | Created VendorReminderLog entity (Id, VendorId FK, ThresholdDays, ExpiryDate, SentAt + BaseEntity audit) | Idempotency log so a reminder for a given (vendor, threshold, expiry cycle) is sent exactly once. Tying the unique key to ExpiryDate means a renewal (which moves the expiry forward) naturally resets the cycle and a new set of reminders will fire |
| 2026-04-23 | DB/Model/ProcuraDbContext.cs | Registered DbSet\<VendorReminderLog\> VendorReminderLogs. Configured Vendor FK with cascade delete and a unique composite index UX_VendorReminderLogs_Vendor_Threshold_Expiry on (VendorId, ThresholdDays, ExpiryDate) | The unique index enforces idempotency at the DB layer so even a race between two worker instances cannot insert duplicates |
| 2026-04-23 | DB/Repositories/VendorRepository.cs | MarkVendorApprovedAsync now also sets RegistrationExpiryDate = approvalUtc.AddYears(3). SendApprovalConfirmationAsync prefers vendor.RegistrationExpiryDate over the on-the-fly compute, falling back to compute only when the column is null (approvals created before this commit) | Persist the expiry so the reminder worker has a durable, queryable anchor. Computed fallback keeps pre-existing approved rows working for the email without a backfill script |
| 2026-04-23 | DB/Repositories/Interfaces/IVendorRepository.cs, DB/Repositories/VendorRepository.cs | Added GetVendorsWithExpiryOnAsync(DateTime targetDateUtc) — filters Approved, IsActive, RegistrationExpiryDate.Date == target. HasRenewalReminderBeenSentAsync(vendorId, thresholdDays, expiryDate) and RecordRenewalReminderSentAsync(…) for per-cycle idempotency | Three repo primitives the worker needs. Kept the filter in SQL (single query per threshold) instead of loading all vendors client-side per Phase 2 audit 2.1 |
| 2026-04-23 | DB/Helper/EmailHelper.cs | Added GetRenewalReminderEmailBody(companyName, vendorCode, expiryDate, daysRemaining, renewalUrl). Header/button color shifts to red (#C0392B) for daysRemaining \<= 7; headline adapts for day-1 wording | Single template covers all 6 thresholds; urgency is a property of the data, not the template identity |
| 2026-04-23 | BusinessLogic/Interfaces/IVendorService.cs, BusinessLogic/Services/VendorService.cs | Added SendRenewalReminderAsync(int vendorId, int daysRemaining). Reads vendor, picks VendorRenewalUrl (fallback to VendorPortalUrl), enqueues via IEmailService. Try/catch + log so the worker loop can continue on any single failure | Subject line includes "URGENT: " prefix when daysRemaining \<= 7 so inbox sort/filters can surface the critical reminders |
| 2026-04-23 | Api/Workers/VendorReminderWorker.cs | Created BackgroundService that runs daily at 02:00 UTC. For each threshold in {90, 30, 14, 7, 3, 1}, queries GetVendorsWithExpiryOnAsync(today + threshold), skips vendors with a pre-existing log row, enqueues email via IVendorService and records the log row. Each iteration uses its own scope. All per-vendor failures are caught and logged without aborting the loop | Chose a lightweight IHostedService over Hangfire/Quartz: 6 fixed thresholds + daily tick does not justify the dependency weight, and the unique index on VendorReminderLog guarantees exactly-once semantics even across restarts |
| 2026-04-23 | Api/Program.cs | Registered VendorReminderWorker as HostedService next to EmailOutboxWorker | Wire the worker into the ASP.NET Core host lifecycle |
| 2026-04-23 | Api/appsettings.json | Added AppSettings.VendorRenewalUrl = "https://portal.fpmsb.com.my/renew" | Dedicated CTA target for the renewal reminder email; falls back to VendorPortalUrl when unset |
| 2026-04-23 | DB/Migrations/ (run `dotnet ef migrations add AddRenewalReminders --project DB --startup-project Api`) | Pending EF Core migration: adds nullable RegistrationExpiryDate (datetime2) to Vendors; creates VendorReminderLogs table (Id PK identity, VendorId FK cascade to Vendors, ThresholdDays int, ExpiryDate datetime2, SentAt datetime2 + BaseEntity columns); adds unique index UX_VendorReminderLogs_Vendor_Threshold_Expiry | Schema changes must be applied before the worker fires. Until the migration runs, the worker queries throw and its catch block logs without side effects; vendor/SAP flows continue to work |
| 2026-04-23 | DB/Model/Vendor.cs | Added nullable DateTime? LastRenewedOn | Captures the timestamp of the most recent successful renewal; surfaced in the renewal success email and useful for reporting. Distinct from ApprovalDatetime (which should stay anchored to the original approval) |
| 2026-04-23 | DB/Repositories/Interfaces/IVendorRepository.cs, DB/Repositories/VendorRepository.cs | Added RenewRegistrationAsync(int vendorId, DateTime renewalUtc, DateTime newExpiryUtc) that sets RegistrationExpiryDate + LastRenewedOn, and flips VendorCodeStatus back to Approved when the previous state was Expired | Single atomic save of the renewal. Automatic Expired → Approved transition means an expired vendor who renews is re-activated in the same call without a separate admin step |
| 2026-04-23 | DB/Helper/EmailHelper.cs | Added GetRenewalSuccessEmailBody(companyName, vendorCode, renewedOn, newExpiryDate, loginUrl) — FPMSB card with a green confirm-box ("Your vendor registration has been successfully renewed") and the new expiry date | Template for the post-renewal confirmation email. Green accent distinguishes it visually from the approval (orange) and reminder (orange/red) templates |
| 2026-04-23 | BusinessLogic/Interfaces/IVendorService.cs, BusinessLogic/Services/VendorService.cs | Added RenewRegistrationAsync(int vendorId) and SendRenewalSuccessAsync(int vendorId). The orchestration method validates the vendor exists, has a prior ApprovalDatetime, and is in Approved or Expired state; computes the new expiry as max(today, currentExpiry) + 3 years; persists via the repo; then calls SendRenewalSuccessAsync | Extending from max(today, currentExpiry) means a vendor who renews early preserves the remaining validity (no penalty for renewing ahead of time), while an expired vendor's new window starts from today. Email call is sequenced after persist; its own try/catch prevents email failures from bubbling up |
| 2026-04-23 | Api/Controllers/VendorController.cs | Added POST /api/Vendor/RenewRegistration?vendorId=... with [Authorize], CSAResponseModel\<bool\> wrapper, and structured exception handling (InvalidOperationException → 400 with the original message; other exceptions → 400 with a generic message + server-side log) | Phase 2 rule-compliant endpoint (authorized, wrapped, try/catch). Separating InvalidOperationException from the generic catch surfaces validation failures ("already approved", "wrong state") to the client while hiding internal errors |
| 2026-04-23 | DB/Migrations/ (fold into AddRenewalReminders or add new `dotnet ef migrations add AddVendorLastRenewedOn --project DB --startup-project Api`) | Pending migration: nullable LastRenewedOn (datetime2) on Vendors. If the AddRenewalReminders migration has not been applied yet you can regenerate it to include this column; otherwise add a separate migration | Nullable so existing rows don't need a backfill; a vendor's LastRenewedOn is null until their first renewal |
| 2026-04-23 | Api/Controllers/AdminController.cs | Changed DeleteRoleMenuPermission verb from [HttpDelete] to [HttpPost] | IIS deployment at 103.27.86.226 returns 405 for DELETE (WebDAVModule intercepts before ASP.NET Core sees the request). Existing AdminController writes all use [HttpPost]; switching aligns with project convention and unblocks the endpoint without requiring a server web.config change. Note: VendorController.DeleteVendor still uses [HttpDelete] and will have the same 405 problem in this environment — fix on a separate change when the DeleteVendor endpoint is actually exercised |
| 2026-04-23 | BusinessLogic/BusinessLogic.csproj | Bumped Microsoft.Extensions.Configuration.Abstractions / DependencyInjection.Abstractions / Logging.Abstractions from 7.0.0 to 9.0.2 | DB transitively brings in Microsoft.Extensions.Configuration 9.0.2 (which requires Abstractions \>= 9.0.2) and Api's Serilog.AspNetCore 9.0.0 brings DI.Abstractions at 9.0.0. The earlier 7.0.0 pins caused NU1605 (package downgrade) errors since NU1605 is treated as warning-as-error. Build now green with 0 errors |
| 2026-04-23 | BusinessLogic/Services/VendorService.cs | SendRegistrationConfirmationAsync now early-returns unless vendor.IsRegistrationComplete == true | The payment repo sets IsRegistrationComplete = true when payment completes. Gating the email on that flag ensures the registration-received email goes out only once the vendor has genuinely completed the multi-step registration + payment flow, regardless of how SendRegistrationConfirmationAsync is invoked (PaymentService.SaveVendorPaymentAsync, SaveVendorPaymentMockAsync, or any future caller). Safer than tying the guard to a specific caller |
